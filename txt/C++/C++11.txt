// lambda表达式
lambda表达式的引用传递只适用于栈对象，当表达式内需要修改该变量时，使用引用传递
堆内指针对象直接做值传递，因为指针即代表对象，所以表达式内外均为同一对象


// C++11新规范
按照C++11新标准，静态局部变量在初始化时，编译器必须保证其线程安全性，即不再需要人为加线程锁。
按照此功能，即可非常安全的实现单例模式
T *getInstance() {
    static T t;		// 永远唯一，且编译器保证其线程安全性
    return &t;
}



